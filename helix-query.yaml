version: 1

indices:
  site:
    include:
      - '/**'
    target: /query-index.json
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, 'content')
      image:
        select: head > meta[property="og:image"]
        value: |
          match(attribute(el, 'content'), 'https:\/\/[^/]+(\/.*)')
      theme:
        select: head > meta[name="theme"]
        value: |
          attribute(el, 'content')
      lastModified:
        select: none
        value: |
          parseTimestamp(headers['last-modified'], 'ddd, DD MMM YYYY hh:mm:ss GMT')
<<<<<<< HEAD
    browse:
    include:
      - /en/browse/**
    target: /browse-all-index.json
=======
  browse:
    include:
      - /en/browse/**
    target: /browse-index.json
>>>>>>> bb451f8afbded7274514f343d175aeb6db828f34
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, 'content')
      theme:
        select: head > meta[name="theme"]
        value: |
          attribute(el, 'content')
      lastModified:
        select: none
        value: |
          parseTimestamp(headers['last-modified'], 'ddd, DD MMM YYYY hh:mm:ss GMT')
